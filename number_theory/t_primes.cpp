#include <iostream>
#include <vector>

using namespace std;

// complexity of the sieve approach
// generating the sieve is O(sqrt(d)log(log(sqrt(d)))) -> 10^6 * sth very small
// then we go over n numbers, and for each number we do O(1)
// the full cost O(sqrt(d)log(log(sqrt(d))) + n) -> (10^6 + 10^5) -> almost 10^6


int main() {
    
}